{
  "name": "redux-react-app",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "react-scripts": "3.2.0"
  },
  "dependencies": {
    "json-server": "^0.15.1",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-redux": "^7.1.3",
    "react-router-dom": "^5.1.2",
    "redux": "^4.0.4",
    "redux-thunk": "^2.3.0"
  },
  "scripts": {
    "start": "json-server --static ./build db.json",
    "dev": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "server": "json-server -p 8080 db.json"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "homepage" : "https://github.com/stefcot/redux-react-app",
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
